# TaskManager
A TaskManager which uses Java SpringBoot as its backend and ReactJs as its frontend. It allows users to store their tasks and displays the list of tasks for the user to plan their schedule.
